{
  "appId": "com.boostinghost.app",
  "appName": "Boostinghost",
  "webDir": "public",
  "plugins": {
    "SplashScreen": {
      "launchShowDuration": 2000,
      "launchAutoHide": true,
      "backgroundColor": "#3B82F6",
      "androidSplashResourceName": "splash",
      "showSpinner": false
    },
    "StatusBar": {
      "style": "light",
      "backgroundColor": "#3B82F6",
      "overlaysWebView": false
    },
    "Haptics": {
      "enabled": true
    }
  }
}
```

**J'ai ENLEVÉ la section `"server"` !**

---

## 💡 POURQUOI ?

### Avec `"server.url"` :
```
App ouvre → Charge https://lcc-booking-manager.onrender.com
         → capacitor.js n'est pas dans le serveur distant
         → Capacitor undefined
         → Face ID ne marche pas
```

### Sans `"server.url"` :
```
App ouvre → Charge les fichiers locaux dans ios/App/App/public/
         → capacitor.js est inclus
         → Capacitor disponible ✅
         → Face ID marche ! 🎉
